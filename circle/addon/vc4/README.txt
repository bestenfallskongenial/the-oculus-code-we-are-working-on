okay

folder h264 decoder

    my attempt of a bare metal wire protocol minimal fixed codec h264 file decoder
    works for:

    - raw h264 files
    - baseline level 3.1 
    - system resolution ( 640 to 480 )

folder h264 parser

    my attempt for a parser that also creates an extradata field to set up the input/output ports

folder vc_sm

    my attempt of a vc_sm driver for zero copy 